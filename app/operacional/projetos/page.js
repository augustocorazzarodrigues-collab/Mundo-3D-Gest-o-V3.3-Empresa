'use client';
import { useEffect, useMemo, useState } from 'react';
import AppShell from '@/components/AppShell';
import Hero from '@/components/Hero';
import { getProjectCode, moneyBR, nameById, sortCreated, newId } from '@/lib/format';
import { listRows, insertRow, updateRow, deleteRow } from '@/lib/service';
const emptyForm={ id:'', name:'', client_id:'', product_id:'', type:'Padrão', status:'Ativo', description:'', target_price:'' };
export default function ProjetosPage(){ const [items,setItems]=useState([]); const [customers,setCustomers]=useState([]); const [products,setProducts]=useState([]); const [form,setForm]=useState(emptyForm); const [search,setSearch]=useState(''); const [error,setError]=useState(''); async function load(){ try{ const [c,p,prj]=await Promise.all([listRows('customers'), listRows('products'), listRows('projects')]); setCustomers(c); setProducts(p); setItems(sortCreated(prj)); } catch(e){ setError(e.message); } } useEffect(()=>{ load(); },[]); function code(){ return form.id ? items.find((p)=>p.id===form.id)?.code || '' : getProjectCode(items.length); } async function saveItem(e){ e.preventDefault(); const payload={ id:form.id || newId('prj'), code:code(), name:form.name, client_id:form.client_id, product_id:form.product_id, type:form.type, status:form.status, description:form.description, target_price:Number(form.target_price||0) }; try{ if(form.id) await updateRow('projects', form.id, payload); else await insertRow('projects', payload); await load(); setForm(emptyForm); } catch(e){ setError(e.message); } } async function deleteItem(id){ try{ await deleteRow('projects', id); await load(); if(form.id===id) setForm(emptyForm); } catch(e){ setError(e.message); } } const filtered=useMemo(()=>items.filter((item)=>[item.code,item.name,nameById(customers,item.client_id),nameById(products,item.product_id),item.status].join(' ').toLowerCase().includes(search.toLowerCase())),[items,customers,products,search]); return <AppShell><Hero kicker="Operacional" title="Projetos" description="Use projetos para organizar itens padrão ou personalizados e ligar com pedidos e O.P." />{error ? <section className="section alert-box">{error}</section> : null}<section className="section surface"><div className="panel-header"><div><div className="panel-title">Gerenciamento</div><div className="panel-subtitle">Cadastro de projetos ligado ao comercial e à produção.</div></div><button className="btn" onClick={()=>setForm(emptyForm)}>Novo registro</button></div><div className="panel"><form onSubmit={saveItem}><div className="row"><div className="field col-2"><label>Código</label><input disabled value={code()} /></div><div className="field col-4"><label>Projeto</label><input value={form.name} onChange={(e)=>setForm({ ...form, name:e.target.value })} required /></div><div className="field col-3"><label>Cliente</label><select value={form.client_id} onChange={(e)=>setForm({ ...form, client_id:e.target.value })}><option value="">Selecione...</option>{customers.map((c)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field col-3"><label>Produto</label><select value={form.product_id} onChange={(e)=>setForm({ ...form, product_id:e.target.value })}><option value="">Selecione...</option>{products.map((p)=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div><div className="field col-3"><label>Tipo</label><select value={form.type} onChange={(e)=>setForm({ ...form, type:e.target.value })}><option>Padrão</option><option>Personalizado</option></select></div><div className="field col-3"><label>Status</label><select value={form.status} onChange={(e)=>setForm({ ...form, status:e.target.value })}><option>Ativo</option><option>Em desenvolvimento</option><option>Aprovado</option><option>Arquivado</option></select></div><div className="field col-3"><label>Preço alvo</label><input type="number" step="0.01" value={form.target_price} onChange={(e)=>setForm({ ...form, target_price:e.target.value })} /></div><div className="field col-12"><label>Descrição</label><textarea value={form.description} onChange={(e)=>setForm({ ...form, description:e.target.value })} /></div></div><div className="actions-row"><button className="btn">Salvar</button><button className="btn-secondary" type="button" onClick={()=>setForm(emptyForm)}>Cancelar</button></div></form></div></section><section className="section surface"><div className="table-tools"><input placeholder="Buscar na tabela..." value={search} onChange={(e)=>setSearch(e.target.value)} /><div className="subtle">{filtered.length} registro(s)</div></div><div className="table-wrap"><table><thead><tr><th>Código</th><th>Projeto</th><th>Cliente</th><th>Produto</th><th>Tipo</th><th>Status</th><th>Preço alvo</th><th>Ações</th></tr></thead><tbody>{filtered.map((item)=><tr key={item.id}><td>{item.code}</td><td>{item.name}</td><td>{nameById(customers,item.client_id)}</td><td>{nameById(products,item.product_id)}</td><td>{item.type}</td><td>{item.status}</td><td>{moneyBR(item.target_price)}</td><td><div className="inline-actions"><button className="btn-secondary" onClick={()=>setForm(item)}>Editar</button><button className="btn-danger" onClick={()=>deleteItem(item.id)}>Excluir</button></div></td></tr>)}</tbody></table></div></section></AppShell>; }
